
name: first robot

on:
  push:
    branches: [main]
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v4
      - name: greetings
        run: echo "hello world"